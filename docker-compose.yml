version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENV=production
      - DATABASE_URL=sqlite:///./sauda.db
    restart: unless-stopped
volumes:
  sauda-data:
    driver: local
